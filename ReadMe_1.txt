If for various problems the “run.bat” file does not work open a terminal inside the folder (right click and then open terminal) and type inside “python3 BloodSpammer.py”
----
before starting “run.bat” start “setup.bat”, to install all the libraries needed to use the tool 
----
this tool was created for Raid the servers of scammers with webhooks